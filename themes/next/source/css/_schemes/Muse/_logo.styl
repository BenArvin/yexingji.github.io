.custom-logo {
  .site-meta-headline { text-align: center; }

  .brand { background: none; }

  .site-title {
    font-size: 24px;
    color: $black-deep;
    a { border: none; }
  }


}

.custom-logo-image {
  width: auto;
  heigth: auto;
  max-height: 150px;
  max-width: 150px;
  background: white;
}
